# Bounded Main Campaign v3 Admission

Thai companion: `BOUNDED_MAIN_CAMPAIGN_V3_ADMISSION.th.md`

Protocol `bounded_whole_vehicle_main_campaign_v3`, campaign `FU-BMC-003`, supersedes v2 after v2 main admission failed closed before ledger initialization. The only new remediation routes protocol-file identity validation through the exact active `--protocol` path. Scientific-rule equivalence across v1/v2/v3 is tested; prior observations are not reused.

Fresh v3 burn-in seed `55999` passed with training reservations/results/pending `240/240/0`, stream counts `80/80/80`, `80` unique GRID proposals, `189` feasible and `51` structural-failure training outcomes, six promotions with zero shortfall, terminal holdout/refinement/CAD evidence `6/6/6`, refinement and STEP/FreeCAD passes `6/6`, and supported finishers in all three treatment streams. Process resume was exact at `1/1/0`.

Training fingerprint: `99c43aa5b8c6dfd64e13b48da47288f2081afe9159a1fc0d76de44f2f2b08eac`. Stage fingerprint: `47e6a64109492cb3af077c76c0b602f9ebc4bb99b30266f226d88def96bbed65`. Decision: `burn_in_accepted_for_admitted_main_campaign`.

Main execution remains allowed only for the exact committed implementation after clean-tree replay and an active-v3-path admission check before ledger initialization. The evidence remains bounded Level-0 plus linear-beam refinement and STEP/FreeCAD transfer evidence, not physical validation or safety certification.
