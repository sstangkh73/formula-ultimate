# Multiscale Discovery Search V1

Thai companion: `MULTISCALE_DISCOVERY_SEARCH_V1.th.md`

Status: Implemented by Work 124 as bounded Level-0 accounting/search evidence.

## Provenance, budgets and archives

This contract pins exact Work 109 representation, Work 117 feedback and Work 123 transient identities. Two policies receive the same immutable external task, eight-candidate library, paired seeds, hardware opportunity and CAD/mesh/solver/tuning/audit partition budgets. Every call reserves registered compute units before execution. Executed failures, retries and score-independent audits remain charged; exhaustion creates `not_evaluated_budget_exhausted` without executing the call.

Admission summaries have exact SHA-256 identities. Cache keys include candidate admission and dependency identities; upstream change invalidates the cache. Feasible, near-feasible and unresolved archives are scope typed. Unknown count includes unresolved and not-evaluated candidates and must remain nonzero.

Continuous shape benefit is a signed effect minus uncertainty and does not require graph novelty. An inactive appendage has zero functional effect regardless of its claimed score. Audits select registered field and B-rep candidates independently of score.

## Replay and claim boundary

Exact decision replay covers archives, not-evaluated records and the complete ledger. Upstream numerical replay identities are retained but are not re-executed by this work. Tampered summaries and unreserved execution fail closed.

Work 123 blocks vehicle promotion. Bounded candidate outcomes/diversity do not establish novelty, discovery, vehicle benefit or physical validation. No optimizer or representation is mandatory, and proposals beyond budget are reported rather than silently discarded.
