# ดัชนีหลักฐาน

ฉบับภาษาไทยของ `EVIDENCE.md`

ทุกข้ออ้างเกี่ยวกับโครงการนี้ ไม่ว่าจะปรากฏใน CV บทคัดย่อ หรือเอกสารส่งประกวด
ควรตรวจสอบได้โดยผู้อ่านที่ไม่มีทางติดต่อผู้พัฒนาและไม่มีเหตุผลจะต้องเชื่ออะไรล่วงหน้า
ไฟล์นี้ระบุข้ออ้าง หลักฐานที่รองรับ และคำสั่งที่ใช้ทำซ้ำผลนั้น

Repository: https://github.com/sstangkh73/formula-ultimate
ORCID ผู้พัฒนา: https://orcid.org/0009-0000-2979-1916
สัญญาอนุญาต: โค้ดใช้ MIT (`LICENSE`) เอกสารและข้อมูลใช้ CC BY 4.0 (`LICENSE-DATA.md`)

## ขนาดของโค้ดเบส

| ข้ออ้าง | วิธีตรวจ |
| --- | --- |
| จำนวนโมดูล Python | `git ls-files '*.py' \| wc -l` |
| จำนวนบรรทัด Python | `git ls-files '*.py' \| xargs cat \| wc -l` |
| จำนวนไฟล์ทดสอบอัตโนมัติ | `git ls-files 'tests/*.py' \| wc -l` |
| ชุดทดสอบผ่าน | `python -m pip install -e . && python -m unittest discover -s tests -v` |

ตัวเลขเหล่านี้คือจำนวนไฟล์จริง ณ commit ปัจจุบัน ไม่ใช่ค่าประมาณและไม่ใช่ตัวเลขที่แช่แข็งไว้ในเอกสาร
ตัวเลขใดที่อ้างไว้ในที่อื่นควรตรงกับผลของคำสั่งข้างต้น หากไม่ตรง ให้ถือว่าคำสั่งถูก
และเอกสารนั้นล้าสมัยแล้ว

## การทดสอบอัตโนมัติต่อเนื่อง (CI)

ชุดทดสอบรันทุกครั้งที่ push และทุก pull request ผ่าน GitHub Actions:
[`.github/workflows/tests.yml`](.github/workflows/tests.yml)
ประวัติการรันดูได้ที่ https://github.com/sstangkh73/formula-ultimate/actions

### สถานะปัจจุบัน

ณ วันที่ 7 กันยายน 2569 ชุดทดสอบผ่านทั้งสองแพลตฟอร์ม

| สภาพแวดล้อม | ผล |
| --- | --- |
| เครื่องพัฒนา Windows, Python 3.14 | `Ran 794 tests` — **OK (skipped=8)** |
| CI, Ubuntu, Python 3.11 และ 3.14 | `Ran 794 tests` — **OK (skipped=32)** |

จำนวนที่ข้ามเพิ่มบน CI คือการทดสอบที่ต้องเล่นซ้ำหลักฐานใน `artifacts/`
ซึ่ง `.gitignore` ตัดออกเพราะขนาดใหญ่ การทดสอบเหล่านี้จะข้ามพร้อมระบุชื่อไฟล์ที่ขาด
แทนที่จะพัง ทำให้ checkout ใหม่รันจนเขียวได้ และเมื่อมีไฟล์ครบ
การทดสอบเดิมก็ยังรันและตรวจสิ่งเดิมทุกประการ

### ความต่างข้ามแพลตฟอร์มที่เคยทำให้ CI แดง และวิธีที่แก้

ก่อนวันที่ 7 กันยายน 2569 มีสองการทดสอบที่ผ่านบน Windows แต่พังบน Linux
ทั้งคู่เปรียบเทียบค่า SHA-256 ของผลการจำลองกับค่าที่บันทึกไว้
สาเหตุถูกค้นด้วยการวัดจริง ไม่ใช่การเดา และวิธีนี้นำกลับมาใช้ได้อีก
[`tools/xplat_digest_probe.py`](tools/xplat_digest_probe.py)
จะพิมพ์ทุกค่ากลางเป็นเลขฐานสิบหกของ float เรียงจากชั้นล่างสุดขึ้นมา
เมื่อนำผลของสองแพลตฟอร์มมา diff กันจึงชี้ได้ว่าชั้นไหนแตกก่อน

สิ่งที่ probe แสดง ตามลำดับ

1. ค่าคอนฟิกที่อ่านจากไฟล์ สถานะเริ่มต้น และสามสเต็ปแรก **เหมือนกันทุกบิต**
   แสดงว่าอินพุตไม่ใช่ปัญหา และไม่ใช่เรื่องรูปแบบขึ้นบรรทัดใหม่หรือการอ่าน JSON
2. รัน probe บน Linux ทั้ง Python 3.11 และ 3.14 ได้ผล **เหมือนกันเป๊ะ**
   จึงตัดเรื่องเวอร์ชันของตัวแปลภาษาและเวอร์ชัน numpy ออกไป
3. การไล่ trace ทุกการเรียกฟังก์ชันคณิตศาสตร์ ชี้ค่าแรกที่ต่าง
   คืออาร์กิวเมนต์ของการคำนวณแรงยาง ต่างกันหนึ่งหน่วยในตำแหน่งสุดท้าย
4. การแก้ระบบหาโหลดล้อที่อยู่เหนือขึ้นไปเหมือนกันทุกบิต
   ความต่างจึงอยู่ที่แรงที่ร้องขอ ไม่ใช่ที่โหลด
5. แรงที่ร้องขอมาจากกฎการลื่นของล้อ ซึ่งคำนวณด้วย **`math.tanh`**
   มาตรฐาน IEEE 754 ไม่ได้บังคับให้ฟังก์ชันมูลฐานปัดเศษถูกต้องที่สุด
   ไลบรารีของ Windows (UCRT) กับ Linux (glibc) จึงต่างกันหนึ่งหน่วยสุดท้าย
   ใน **35 จาก 111 จุดที่สุ่มทดสอบ** และใน **9 จาก 40 การเรียกแรกสุด**
   ที่แบบจำลองนี้เรียกจริง ค่าแรกที่ต่างในการรันทั้งหมดคือ
   `tanh(0x1.3755407c17994p-10)` ซึ่งได้ `...385` บน Windows และ `...386` บน Linux

หนึ่งหน่วยสุดท้ายก็เพียงพอ เพราะความร้อนจากการลื่นตามยาวคำนวณจาก
ผลต่างของสองจำนวนที่เกือบเท่ากัน จึงขยายความต่างขึ้นราวสองร้อยเท่าภายในไม่กี่สเต็ป
แล้วลามไปถึงความเร็วเชิงอนุพันธ์ สถานะที่บันทึก และค่า digest ในที่สุด

การแก้คือคำนวณ tanh ด้วยเลขทศนิยมแบบ decimal
([`src/formula_ultimate/physics/deterministic_math.py`](src/formula_ultimate/physics/deterministic_math.py))
ซึ่งเป็นการคำนวณด้วยซอฟต์แวร์ที่ให้ผลแน่นอน ไม่เรียกไลบรารีของระบบปฏิบัติการ
ค่า digest ของงาน 073 และ 074 ถูกบันทึกใหม่เพราะการคำนวณเปลี่ยนไปจริง
ส่วนหลักฐานเดิมใน `artifacts/` ยังเก็บค่าที่กฎการลื่นแบบเก่าให้ไว้ตามเดิม ไม่ถูกแก้
และ [`tests/test_deterministic_math.py`](tests/test_deterministic_math.py)
ตรวจรูปแบบบิตที่แน่นอน เพื่อบังคับคุณสมบัตินี้ไว้ ไม่ใช่แค่สมมติว่ายังเป็นจริง

### สิ่งที่ยังเป็นความจริงเรื่องการทำซ้ำผลลัพธ์

แบบจำลองยังเรียกฟังก์ชันมูลฐานอื่นอีก ได้แก่ `cos` 28 ครั้ง `sin` 26 ครั้ง
`atan2` 9 ครั้ง และอื่น ๆ ซึ่งมีความเสี่ยงแบบเดียวกัน
ฟังก์ชันเหล่านี้ให้ผลตรงกันระหว่างสองแพลตฟอร์มที่อาร์กิวเมนต์ซึ่งแบบจำลองไปถึงในตอนนี้
และผล CI ที่เขียวก็เป็นหลักฐานของข้อนั้น แต่ไม่ใช่ข้อพิสูจน์ว่าจะตรงกันเสมอไป
การที่ digest ที่บันทึกจากเครื่องหนึ่งทำซ้ำได้บนอีกเครื่องหนึ่ง
จึงเป็น **ผลเชิงประจักษ์สำหรับคอนฟิกที่ทดสอบแล้ว** ไม่ใช่การรับประกันสำหรับทุกคอนฟิกในอนาคต
การพูดเกินกว่านี้คือการเคลมเกินสิ่งที่แสดงได้จริง

## ข้ออ้างเชิงวิจัย

| ข้ออ้าง | นิยามหรือการบังคับใช้อยู่ที่ไหน |
| --- | --- |
| เอเจนต์ออกแบบยานพาหนะ 3 มิติทั้งคันโดยไม่กำหนดสถาปัตยกรรมแบบเดิมล่วงหน้า | คำถามวิจัยและข้อจำกัดใน `README.md` และไปป์ไลน์การค้นพบใน `src/` |
| กติกาการแข่งและงบพลังงานอ้างอิงกฎ FIA Formula One ฉบับ 2026 | `README.md` อ้าง Sporting Regulation B5.1.4 และ Technical Regulation C6.4.4 พร้อมลิงก์เอกสารต้นทางของ FIA |
| พลังงานขับเคลื่อนหลักต้องบรรทุกไปทั้งหมดก่อนออกสตาร์ต และเติมระหว่างแข่งไม่ได้ | เขียนเป็นข้อจำกัดที่ถูกตรวจจริง ไม่ใช่แค่ประกาศเจตนา ดูการบัญชีพลังงานใน `src/` และการทดสอบใน `tests/` |
| ผลลัพธ์จะถูกยอมรับก็ต่อเมื่อผ่านการตรวจฟิสิกส์หลายระดับความละเอียด | ด่านตรวจหลักฐานใน `src/` ซึ่งถูกเรียกใช้โดยชุดทดสอบ |
| แบบที่ชนะไม่สามารถชนะด้วยการหาช่องโหว่ของตัวจำลอง | ด่านตรวจ conservation residual และด่านตรวจความถูกต้อง ดูการทดสอบด่านเหล่านี้ใน `tests/` |

## นิยามของคำว่า "เร็วที่สุด" ในโครงการนี้

เป้าหมายคือเวลารวมในการแข่งที่ต่ำที่สุด **ในบรรดาแบบที่วิ่งจบการแข่งตามกติกาที่ประกาศไว้
และผ่านด่านตรวจหลักฐานที่กำหนด** ไม่ใช่ความเร็วสูงสุด และไม่ใช่การวิ่งได้ชั่วครู่
ก่อนโครงสร้างหรือระบบระบายความร้อนจะพัง

ความแตกต่างนี้ถูกเขียนไว้ใน `README.md` เพราะเป้าหมายที่ไม่ได้นิยามไว้ ย่อมตรวจสอบไม่ได้

## สิ่งที่ไม่ได้อ้าง

- ไม่มีการอ้างว่าผลลัพธ์ใดเอาชนะรถ Formula One จริงได้ การเปรียบเทียบทั้งหมดเทียบกับ
  baseline แบบดั้งเดิมที่ปรับแต่งอย่างเป็นธรรม ภายในสัญญาการจำลองที่ประกาศไว้เท่านั้น
- ความละเอียดของฟิสิกส์เป็นแบบแบ่งชั้นและมีขอบเขต การผ่านด่านปัจจุบันหมายความเพียงว่า
  แบบนั้นรอดผ่านระดับความละเอียดเท่าที่พัฒนาไว้แล้ว ซึ่งระบุไว้ในรายงานตรงไปตรงมา
  ไม่ได้ทำให้เข้าใจว่าครบถ้วนสมบูรณ์
- หากคำถามวิจัยใดยังเปิดอยู่ `docs/` จะบันทึกไว้เป็นแผน ไม่ใช่ข้อค้นพบ
  เอกสารใน `docs/plans/` คือความตั้งใจ ส่วนผลลัพธ์อยู่ในรายงานเท่านั้น
