# Configuration

Version-controlled fidelity, vehicle-envelope, track, component-catalog, and
experiment settings belong here. Configuration values must use SI units or
carry an explicit unit suffix. Every research run records the configuration
content hash.

Numerical defaults will be added only with the implementation milestone that
validates them.
